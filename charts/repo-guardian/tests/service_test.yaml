suite: service
templates:
  - templates/service.yaml
tests:
  - it: should create a ClusterIP service by default
    asserts:
      - equal:
          path: spec.type
          value: ClusterIP

  - it: should expose http and metrics ports
    asserts:
      - contains:
          path: spec.ports
          content:
            port: 80
            targetPort: http
            protocol: TCP
            name: http
      - contains:
          path: spec.ports
          content:
            port: 9090
            targetPort: metrics
            protocol: TCP
            name: metrics

  - it: should use custom ports when set
    set:
      service:
        httpPort: 8080
        metricsPort: 9191
    asserts:
      - contains:
          path: spec.ports
          content:
            port: 8080
            targetPort: http
            protocol: TCP
            name: http
      - contains:
          path: spec.ports
          content:
            port: 9191
            targetPort: metrics
            protocol: TCP
            name: metrics

  - it: should use selector labels
    asserts:
      - isSubset:
          path: spec.selector
          content:
            app.kubernetes.io/name: repo-guardian
            app.kubernetes.io/instance: RELEASE-NAME
