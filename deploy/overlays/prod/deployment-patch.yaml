apiVersion: apps/v1
kind: Deployment
metadata:
  name: repo-guardian
spec:
  template:
    spec:
      containers:
        - name: repo-guardian
          env:
            - name: DRY_RUN
              value: "false"
            - name: LOG_LEVEL
              value: info
            - name: SCHEDULE_INTERVAL
              value: 168h
