apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - tailscale-secret.yaml
  - tailscale-rbac.yaml
  - tailscale-serve-config.yaml

patches:
  - path: deployment-patch.yaml
