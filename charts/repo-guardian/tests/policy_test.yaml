---
suite: policy configmap and deployment integration
templates:
  - templates/policy-configmap.yaml
  - templates/deployment.yaml
set:
  # One Deployment (topology: all) keeps these single-document; the
  # split shape is covered by topology_test.yaml.
  topology: all
  temporal:
    address: temporal:7233
  config:
    appId: "12345"
  secrets:
    webhookSecret: "test-secret"
    privateKey: "test-key"
tests:
  - it: should render policy ConfigMap with inline config
    template: templates/policy-configmap.yaml
    set:
      policy:
        config: |
          guardian {
            log_level = "debug"
          }
    asserts:
      - isKind:
          of: ConfigMap
      - equal:
          path: metadata.name
          value: RELEASE-NAME-repo-guardian-policy

  - it: should not render policy ConfigMap when config is empty
    template: templates/policy-configmap.yaml
    set:
      policy:
        existingConfigMap: my-external-policy
    asserts:
      - hasDocuments:
          count: 0

  - it: should set GUARDIAN_CONFIG env var when policy config is set
    template: templates/deployment.yaml
    set:
      policy:
        config: |
          guardian {
            log_level = "debug"
          }
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: GUARDIAN_CONFIG
            value: /etc/repo-guardian/policy/guardian.hcl

  - it: should set GUARDIAN_CONFIG env var when existing ConfigMap is used
    template: templates/deployment.yaml
    set:
      policy:
        existingConfigMap: my-external-policy
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: GUARDIAN_CONFIG
            value: /etc/repo-guardian/policy/guardian.hcl

  - it: fails without a policy, since the worker needs GUARDIAN_CONFIG
    template: templates/deployment.yaml
    asserts:
      - failedTemplate:
          errorPattern: "policy.config or policy.existingConfigMap is required"

  - it: should mount policy volume when config is set
    template: templates/deployment.yaml
    set:
      policy:
        config: |
          guardian {
            log_level = "debug"
          }
    asserts:
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: policy
            mountPath: /etc/repo-guardian/policy
            readOnly: true

  - it: should add policy volume from inline config
    template: templates/deployment.yaml
    set:
      policy:
        config: |
          guardian {
            log_level = "debug"
          }
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: policy
            configMap:
              name: RELEASE-NAME-repo-guardian-policy

  - it: should use external ConfigMap for policy volume
    template: templates/deployment.yaml
    set:
      policy:
        existingConfigMap: my-external-policy
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: policy
            configMap:
              name: my-external-policy
